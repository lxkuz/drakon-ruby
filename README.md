# drakon-ruby

Транслятор из языка **ДРАКОН** (блок-схемы / текстовое представление схемы) в **Ruby**.

Проект в ранней стадии: заготовка гема и CLI `drakon2rb`; парсинг и генерация кода пока не реализованы.

## Установка из исходников

```bash
cd drakon-ruby
bundle install
ruby exe/drakon2rb path/to/file.drakon
```

После публикации гема: `gem install drakon_ruby` (и `drakon2rb` окажется в `PATH`).

## Использование

```bash
drakon2rb схема.drakon
```

Ожидается файл на входе (формат будет описан после поддержки парсера — часто XML `.drakon` из редактора). Текущая версия возвращает ошибку «ещё не реализовано».

## Разработка

Структура:

- `lib/drakon_ruby.rb` — точка входа модуля
- `lib/drakon_ruby/translator.rb` — класс `DrakonRuby::Translator`
- `exe/drakon2rb` — командная строка

Цель транслятора: разбор графа схемы → внутреннее представление → генерация корректного Ruby-кода.

## Лицензия

По согласованию с авторами — отдельный файл лицензии при необходимости.
